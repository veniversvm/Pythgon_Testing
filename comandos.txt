# Descubrir test
py -m unittest discover -s tests d

# Destalle de los test (el orden de las flags es importante)
py -m unittest discover -v -s tests

# Para ejecutar suites de test definidas
PYTHONPATH=. python tests/test_suites.py